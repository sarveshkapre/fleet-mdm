# Release Process

1) Run `make check` and confirm green
2) Update `docs/CHANGELOG.md`
3) Tag release: `git tag vX.Y.Z`
4) Push tags: `git push --tags`
5) Create GitHub release with notes from the changelog
